{%extends 'base.jinja'%}{%block content%}
<h1>Library</h1>
<ul>
  {%for book in books%}  
    <li>
      {{book.author}} - <a href="/books/{{loop.index0}}"><b>{{book.title}}</b></a>
      {%if false in book.checked_out%}
        <span>~available~</span>
      {%endif%}
      <form action="/books/{{loop.index0}}/delete" method="post"><input type="submit" value="Delete"></form>
    </li>
  {%endfor%}
</ul>
{%endblock content%}